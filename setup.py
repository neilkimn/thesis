from setuptools import setup

setup(
    name='thesis',
    install_requires=[
        'black',
        'tensorboardX',
        'torch-tb-profiler',
        'matplotlib'
    ],
    dependency_links=[
    ]
)
