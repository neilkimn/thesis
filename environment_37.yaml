name: thesis3.7
channels:
  - pytorch
  - nvidia
dependencies:
  - python==3.7.15
  - pip
  - pytorch
  - torchvision
  - pytorch-cuda=11.6
  - pip:
      - black
      - tensorboardX
      - opencv-python
      - torch-tb-profiler
      - nvidia-pyindex
      - nvidia-dali-cuda110
      - cupy-cuda110